# immutablehub_pull_architecture
ARCHITECTURE OF GIT PULL ON IMMUTABLEHUB


> All pulls are git powered  which pull the history from  Immutable from Git bundles via git merge, preserving the complete commit graph and repository state.
> Immutable allows availability, cryptographic integrity, and resistance to tampering, takedowns, or centralized failure, so history can be pulled reliably
